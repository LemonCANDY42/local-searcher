# SearXNG port (host-side)
SEARXNG_PORT=8888

# ntfy notification bus port
NTFY_PORT=18082

# Data directory for persistent volumes
SEARCH_INFO_HOME=~/.local-searcher

# SearXNG secret (generate with: openssl rand -hex 32)
SEARXNG_SECRET=change-me-generate-a-real-secret
